# Backend Dockerfile
FROM node:20-slim

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Build frontend assets if needed, but usually we run them separately
# For a full-stack single-container approach:
# RUN npm run build

EXPOSE 3000

CMD ["npm", "run", "dev"]
